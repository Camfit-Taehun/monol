# NOTE: 경로 가정(UC01/UC02)

UC01/UC02 마이크로 태스크는 기본적으로 통합 패키지 루트를 `monol/monol-suite/`로 가정합니다.
하지만 실제로 통합 루트를 `monol/`로 만들었다면, 문서 내 경로를 통합 루트에 맞춰 치환하세요.

- 현재 폴더는 “계획/태스크 문서”만 포함합니다.
- 실제 코드 리포 구조는 각 Task에서 안내하는 대로 정리/이관하세요.
