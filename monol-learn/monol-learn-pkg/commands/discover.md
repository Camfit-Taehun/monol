---
description: "외부 소스에서 새 스킬 발견"
argument-hint: "[source]"
allowed-tools: [Read, Write, Bash, WebFetch, WebSearch, Glob, Grep]
---

# /discover - 새 스킬 발견

외부 소스를 스캔하여 새로운 스킬, 패턴, 기술을 발견합니다.

## 사용법

```
/discover                    # 모든 등록된 소스 스캔
/discover plugins            # 플러그인 마켓플레이스만 스캔
/discover github             # GitHub 트렌드 스캔
/discover <url>              # 특정 URL 스캔
```

## 발견 프로세스

1. **소스 스캔**: 지정된 소스에서 새 컨텐츠 검색
2. **관련성 분석**: 현재 프로젝트와의 관련성 평가
3. **후보 추출**: 잠재적 스킬 후보 식별
4. **중복 제거**: 기존 스킬과 비교하여 중복 제거
5. **등록**: 높은 관련성 후보를 진화 시스템에 등록

## 실행 절차

### 1. 설정 확인

먼저 학습 설정을 확인합니다:

```bash
# 설정 파일 확인
cat .monol/learn/config.yaml 2>/dev/null || echo "설정 파일 없음"
```

### 2. 소스 스캔

지정된 소스 유형에 따라 스캔을 실행합니다:

**plugins** - Claude Code 플러그인 마켓플레이스:
- 새로 등록된 플러그인 확인
- 업데이트된 플러그인 확인
- 인기 상승 플러그인 확인

**github** - GitHub 트렌드:
- 관련 언어/프레임워크의 트렌딩 저장소
- 최근 활발한 코딩 패턴

**url** - 특정 URL:
- 기술 블로그
- 공식 문서
- 튜토리얼

### 3. 후보 평가

각 후보에 대해:
- 관련성 점수 (0-100) 계산
- 신규성 점수 계산
- 유용성 예측

### 4. 결과 보고

발견 결과를 보고합니다:

```markdown
## 발견 결과

### 높은 관련성 후보 (70+)
- [후보명]: 관련성 XX, 신규성 XX

### 관심 후보 (50-70)
- [후보명]: 관련성 XX, 신규성 XX

### 다음 단계
- `/compare` 명령으로 기존 스킬과 비교
- `/absorb` 명령으로 즉시 내재화
```

## 소스 설정

`.monol/learn/sources.yaml`:

```yaml
sources:
  - type: plugin-marketplace
    url: https://claude.ai/marketplace
    scanFrequency: daily

  - type: github
    url: https://github.com/trending
    languages: [typescript, python]
    scanFrequency: daily

  - type: blog
    url: https://engineering.blog/feed
    scanFrequency: weekly
```

## 옵션

- `--min-relevance <n>`: 최소 관련성 점수 (기본: 30)
- `--max-candidates <n>`: 최대 후보 수 (기본: 20)
- `--include-low-confidence`: 낮은 신뢰도 후보도 포함

## 예시 출력

```
🔍 Discovery Scan 완료

📊 스캔 결과:
- 소스 스캔: 3개
- 발견된 후보: 15개
- 높은 관련성: 4개

🎯 상위 후보:
1. advanced-error-handler (관련성: 85, 신규성: 78)
   - 타입: technique
   - 출처: plugin-marketplace

2. test-pattern-matcher (관련성: 72, 신규성: 82)
   - 타입: pattern
   - 출처: github

💡 권장 작업:
- advanced-error-handler: /compare로 기존 에러 처리와 비교 권장
- test-pattern-matcher: /absorb로 즉시 내재화 가능
```
